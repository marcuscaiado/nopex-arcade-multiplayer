services:
  arcade:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nopex-arcade-3d
    ports:
      - "8080:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
